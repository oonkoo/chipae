# Session State — 2026-09-13 (board look, Phase 1)

## Current task

**Empire Wars "Emperor's Map Table" restyle — Phase 1 (CSS materials) DONE,
live-checked on desktop. Phase 2 art being generated by the user. Nothing
committed.** Scope per user: **game board only** — no chat backdrop, no navbar.

### Done
- `app/globals.css`: Empire Wars tokens rebuilt (walnut `--ew-wood*`, brass
  `--ew-brass*`, parchment, ivory, lapis, shine/shade/shadow; enamel bands;
  `--ew-neon` removed) + `@utility` materials: `ew-scope` (re-points platform
  tokens incl. `--game*` → brass button inside the board only; also on portaled
  popovers), `ew-table`, `ew-frame`, `ew-inlay` (octagram placeholder),
  `ew-plaque`, `ew-plaque-ornate`, `ew-card` (parchment), `ew-enamel`,
  `ew-plate`, `ew-panel`, `ew-dialog`, `ew-backdrop`, `ew-chip`. Plaques/panels
  take `--ew-ring` (owner) and `--ew-glow` (here / can build / on clock).
- board.tsx (table, frame, inlay, tile padding, centre inset 12.5%), tile.tsx
  (plaques, enamel band, owner plate, glows, ornate corners, parchment decks,
  popovers), players/prompt/trade/auction panels, setup/toss/how-to-play/
  market dialogs, pieces shadow; icons glyph shade fills tokenised. No colour
  literals left in the board components.
- typecheck + lint clean, 234 tests pass.

### Phase 2 — centerpiece DONE (live-verified desktop + 400 px)
- User's `center-piece-{wide,square}.png` (JPEG, 5.7 MB) → compressed to
  `public/games/empire-wars/board/centerpiece-{wide,square}.webp` (222/181 KB).
  Originals still in `public/games/empire-wars/` — ask before deleting.
- `BoardCenterpiece` in art.tsx; `ew-inlay` replaced by `ew-centerpiece`.
- Ring now snaps to **square or exactly 3:2** via `@custom-variant
  ew-stage-wide` (`@container stage (aspect-ratio > 1.25)`), so the painted
  gilt frame is never cropped; the same variant picks wide vs square art.
- Legibility on light marble: feed in an `ew-panel` ledger, prompt's plain
  states (turn line, manage, winner) and TurnClock/errors on walnut chips.
- Verified live: wide art 3:2 uncropped; setup dialog in new style; at 400 px
  ring 382×382 square art, buy panel/feed/treasury readable.

### Phase 2 — walnut texture DONE (live-verified)
- User's `Walnut wood.png` (JPEG, strong spotlight vignette) → lighting
  flattened (divide by a 10×10-block light field) + seamless (half-offset
  cross-fades x then y) → `board/walnut.webp` 512², 17 KB. Original still in
  `public/games/empire-wars/` — ask before deleting.
- `--ew-walnut` token; `ew-plaque`, `ew-plaque-ornate`, `ew-frame` layer a tint
  gradient over the grain (256 px tiles; frame 320 px). Per-tile
  `--ew-grain` offset (`grainAt(tile)` in tile.tsx) so neighbours differ.

### Phase 2 — parchment + corner ornament DONE (live-verified)
- `Parchment.png` → flattened with a coarse 4×4 light field (keeps mottle) +
  seamless → `board/parchment.webp` 4 KB; `--ew-parchment-tex` in `ew-card`.
- `Gold corner ornament.png` (JPEG on white; painted as a bottom-left piece)
  → cut out → `board/corner.webp` 256², 27 KB. `CornerOrnament` (mirrored per
  corner) on the 4 corner tiles' outer corners (`OUTER_CORNER` in tile.tsx);
  `FrameCorners` in setup/toss/how-to-play/Market dialogs, `-z-10` under the
  content with the dialog `isolate`d.
- Originals `Walnut wood.png`, `Parchment.png`, `Gold corner ornament.png` still
  in `public/games/empire-wars/` — ask before deleting.

### Phase 2 — special tiles DONE (NOT live-verified)
- Two 3-icon sheets → `public/games/empire-wars/specials/{silk-road,
  royal-decree,dungeon,fortune,treasury,treason}.png` (128²). Sheet 2 needed
  enclosed-background clears (cuff interiors, astrolabe loop, chain links) and
  the slicer's new `--cuts=370,695` (coins nearly touch the shackles: pieces
  assigned to columns by centroid). `SPECIAL_ART` now lists all six.
- Verified: cut icons on walnut (no halos/holes), typecheck + lint clean.
- NOT verified live: on 2026-09-14 localhost:3000 was serving another project
  (RentZoro), Chipae's dev server was down — check the special tiles (art on
  parchment cards + corner ornaments) next time it runs.
- Source sheets still in `public/games/empire-wars/` — ask before deleting.

### Source sheets deleted (user approved 2026-09-14)
Walnut wood / Parchment / Gold corner ornament / both special-tile sheets.

### Phase 3 — motion DONE in code (NOT live-verified)
Designed with the motion-design skill (ease-out-quart arrivals; one
orchestrated moment).
- `globals.css`: tokens `--ew-ease-arrive`, `--ew-pulse-ms` 560, `--ew-rise-ms`
  700, `--ew-moment-ms` 2600; keyframes `ew-pulse`, `ew-rise`, `ew-glint`,
  `ew-moment`, `ew-moment-fade`.
- board.tsx: `arrivedAt = useState(view.version)`; fresh events → per-tile
  pulses (tribute → owner colour; buy/auction-won → buyer) + latest monument
  build (`level === MONUMENT_LEVEL`). **New hook in the board → reload any open
  game page (Fast Refresh gotcha).**
- tile.tsx: `PulseRing` (keyed by seq, delay frozen at mount = walk time,
  `forwards` fill so it's hidden during the delay); monument rise + glint in
  `Buildings`; hover = `brightness-115` (no lift).
- `moment.tsx`: `MonumentMoment` card, pointer-events-none, aria-hidden,
  motion-reduce → opacity-only.
- GDD UI table: new **Motion** row. typecheck + lint + 234 tests clean.
- NOT verified live: localhost:3000 serves RentZoro; Chipae dev server down.

### New player pieces (2026-09-14)
- User's `new-pieces-6.png`: real PNG with alpha, 3×2 grid, plus ~16% of the
  sheet a near-invisible alpha haze (1–32) from a background remover. New
  scratchpad `slice-alpha.mjs` (clears alpha < 33, groups shapes by centre) →
  `pieces/<id>.webp` 128², 6–10 KB. Old `pieces/*.png` deleted;
  `PLAYER_PIECES` srcs now `.webp` — new URLs so Next's 4 h image cache can't
  serve the old art. Source sheet deleted (user approved). Gangster-theme
  `tiles/` SVGs (3.2 MB, unreferenced) deleted too (user approved).

### Next
Live check when Chipae runs: new pieces on chips, special tiles, landing pulse,
monument moment (needs a whole empire + 4 houses + monument).

---

# Session State — 2026-09-11 (art)

## Current task

**Empire Wars painted art wired in — DONE and live-verified (1 human + CPU).
Nothing committed.**

User generated art with Gemini (prompts from this session); sheets were cut to
transparent 128×128 PNGs (cities, crests, houses, pieces) — monuments were
already 500×500 with alpha. The prompts came from an empire list drafted in
this session while the chipae-3b session wrote v3 with a different one; the
user then chose **change the code to match the art**:
- Egypt → **Ottoman Empire** (tier 1, same price): Edirne · Bursa · Istanbul,
  Topkapı Palace
- Rome: Carthage/Antioch → **Pompeii/Ravenna**; Mongol: Samarkand → **Sarai**,
  monument → **The Genghis Khan Statue**; Ming: Xi'an → **Guangzhou**,
  monument → **The Forbidden City**; Britain: Edinburgh → **Liverpool**
- Prices/tiers unchanged (calibration stands). State is positional
  (`state.cities` by index), so in-progress games keep ownership — no RULESET
  bump. GDD roster table + examples, tests (2 asserts + comments), glyphs,
  band comment updated. **All art is now wired; only 6 special tiles missing.**

### Done
- `components/game/empire-wars/art.tsx` (new): the art manifest sets
  (`CITY_ART` 24 cities, `EMPIRE_ART` 8 crests+houses, `MONUMENT_ART` 8,
  `SPECIAL_ART` empty) + `EmpireCrest`, `CityArt`, `HouseArt`, `MonumentArt`
  with stand-ins (faded crest / monument silhouette / gold house glyph /
  blank medallion). `empireVar` moved here from tile.tsx. Eager loading.
- Pieces: `PLAYER_PIECES` = crown, galleon, cannon, trebuchet, balloon, lamp
  (PNG); new `PieceChip` in pieces.tsx (piece on a disc + ring of seat colour),
  used by `GamePiece` and the player panels.
- Tile: picture fills the body, right-aligned at @md with the name over its
  top-left (a separate name row left the art 17 px tall); houses/monument
  bottom-left; city dims to 45% under a monument. City card: art thumbnail,
  crest, house/monument art in the tribute ladder. Market + auction: crests
  replace colour dots; auction shows the city art.
- Asset files renamed to game ids: roman→rome, french→france, british→britain.
- `--ew-district-*` reordered (same 8 hexes) so each band matches its crest
  and monument base: Egypt teal, Rome purple, Mongol slate, Mughal red
  sandstone (Ming/France/Britain/Bengal unchanged).
- Docs: `design/empire-wars-art-manifest.md` rewritten (status per file,
  stand-ins, unused files, priority); GDD UI rows updated.

### Verified
- typecheck + lint clean, 234 tests pass.
- Live: all 16 city icons, 5 crest stand-ins, pieces load (no broken images),
  bands match, owned tiles, Rome card ladder shows villas + Colosseum. Only
  console issue is the known `cz-shortcut-listen` extension warning.

### Not verified
- Houses/monument *on a tile* (needs a whole empire built) — the card ladder
  renders the same components.
- Phone-width board with the art.

### Missing art (see manifest)
Only the 6 special tiles (`specials/<kind>.png`). Old gangster SVGs in
`tiles/` are unused (~2.6 MB) — user not yet asked to delete.

---

# Session State — 2026-09-11

## Current task

**Empire Wars match setup — DONE and live-verified (1 human + CPU). Nothing
committed.**

User asked: after joining, everyone picks *Assets* (3K/5K/10K/Unlimited) and
*Rounds* (30/50/100/Unlimited); a random toss settles it; with CPUs the
human's input is used. Answers (all recommended): the toss picks **whose
settings** are used; setup happens **in the game** before the first roll (no
lobby/DB change); Assets = **net-worth target**.

### Done
- `SETUP` in `lib/game/data/empire-wars.ts` (options, defaults 5K·50, 30 s
  window, `safetyRounds` 200). `TARGET_NET_WORTH`, `targetFor` and
  `EMPIRE_WARS_CONFIG.roundLimit` removed.
- Rules: RULESET 4; phase `setup`; state `target`/`roundLimit` nullable +
  `setup` (humans, picks, expiresAt); `settled` event (settledBy
  toss/agreed/default, picks). Moves `pickSetup`, `closeSetup`. Toss = uniform
  over pickers in seat order via ctx.random. Quit mid-setup handled. CPU only
  closes setup at the deadline. Both-unlimited safety net at round 200 (sim
  found 1–5% of inf/inf CPU matches never ended).
- Platform: `startGame` passes `ctx.humans` to `deal` (ADR-0005 Amendment 2
  addendum). No schema change.
- UI: `setup-panel.tsx` (chips, Lock in, picks list, clock), `toss-reveal.tsx`
  (highlight spin → winner; "Your rules"/"Everyone agreed"/"Standard match"),
  board header for unlimited modes, no progress bar without a target,
  how-to-play line 7 from settings and deferred until after the toss, CPU
  driver waits for the reveal.
- GDD: new *Match setup* section, formulas, full 16-combo matrix + standard
  match calibration, edge cases, knobs, acceptance, UI rows, open questions.

### Verified
- typecheck + lint clean; **234 tests pass** (16 new setup tests, 16-combo
  sim finish test, recalibrated bands for 5K·50).
- Standard match, 5,000 seeds/size: 60/91/99/98/95% on target (2–6 seats),
  median round 46/34/28/25/23; 4 seats monument 91%, 2.28 whole empires.
- Live: setup dialog, 5K·50 preselected, lock-in (lone human settles
  instantly), header variants (Unlimited target; 10K + no limit; 3K·30),
  timeout → "Standard match" via advanceBot, reveal appears/hides (DOM
  observer), 380 px felt fits (chips text-xs), no console errors.

### NOT verified
- The toss spin itself — needs 2+ human accounts at one table.
- A stale `08P01` socket hit once after a hot reload (known gotcha; cleared on
  reload).

---

# Session State — 2026-09-10 (late)

## Current task

**Empire Wars v3 — historical empires + Monopoly depth. GDD, code, tests,
calibration DONE; live-verified except building/monuments. Nothing committed.**

Arc today: v1 "too complex" → **v2** (crime-city Monopoly-lite: roll, buy,
rent, race; built and live-tested; UI passes: you bottom-left, bigger board,
full-height 2/3 board + 1/3 chat grid, title/Quit above chat) → user asked to
re-theme as **v3**: color group = historical empire, richest = Bengal
(Sonargaon/Dhaka/Murshidabad), build only with the whole empire (4 houses →
monument), trading + auctions, sell/upgrade on your turn, themed Chance /
Community Chest. User said "go with the recommendations": Market is a button
on your turn (trade + auction own city); a passed/unaffordable city goes to
auction; coins not $; keep the net-worth race; placeholder SVG art.

### Done (v3)
- GDD `design/gdd/empire-wars-core.md` v3, numbers LOCKED by calibration
  (no "provisional" left). v2 archived at `archive/empire-wars-core-v2.md`;
  v1 code backup `archive/empire-wars-v1-code.tar.gz`.
- ADR-0005 **Amendment 2**: `MoveContext.humans?` — `runTransition` in
  `lib/actions/games.ts` passes connected human seats. Used for CPU proxy
  bids and so a timed-out human is never played as a CPU.
- Data `lib/game/data/empire-wars.ts`: 8 empires (Egypt, Rome, Mongol, Ming,
  Mughal, France, Britain, Bengal) × 3 cities, monuments, Fortune + Royal
  Decree decks (10 each), targets 3,200/3,300/3,800/4,300/5,000 (2–6 seats —
  superseded 2026-09-11 by match setup), manage clock 60 s (30 s ended a live turn mid-trade), CPU trade offer 2.5×
  every 2 rounds (2× was never accepted → no empires formed).
- Rules `lib/game/empire-wars/{rules,turn,bot,view,module,simulate}.ts`:
  phases roll|answer|auction|trade|manage; live auctions with CPU proxy
  bidding, close-on-the-spot when nobody can beat a bid; player-auction
  opening bid = half price (a lone CPU had bought a city for 10); trades
  answered by the recipient in 20 s; forced sales buildings-first.
- UI `components/game/empire-wars/*`: board (auction panel, trade card,
  Market dialog with clock, city card with build/sell), how-to-play v3,
  monument SVG silhouettes, pieces (crown/sword/galleon/shield/compass/quill).
- Catalog tagline → "Build an empire. Collect the tribute."
- Art manifest rewritten for v3; systems-index updated.

### Verified
- typecheck + lint clean; **196 tests pass** (10 files).
- Calibration, 5,000 seeds per size: 80/80/83/83/82% end on target (2–6
  seats), median round 26/26/24/23/23; 4 seats: 2.16 whole empires, monument
  in 66%, bankrupt before r12 0.2%, r10 leader wins 31%, 0 rejected moves.
- Live vs CPU "Croupier" (local lobby cms7sqkou00bndoru3i0jly9o): how-to-play,
  buy question, pass → auction with CPU counter-bid, deadline close, Market
  trade accepted by the CPU, money flights, no console errors. Fixed live:
  "You offers" grammar, 30 s clock ending a turn mid-trade.

### NOT verified / open
- Building, monuments, selling, forced sales — unit-tested only, never live.
- Phone layout of v3 not re-checked.
- Monument art arriving in `public/games/empire-wars/monuments/` (user,
  23:39–23:42): french/british/roman/mughal match; **ming = Forbidden City**
  (data says Great Wall), **mongol = horseman statue** (data says Silver
  Tree), **ottoman.png** has no empire on the board; Egypt + Bengal missing.
  All are 2048² JPEGs named .png, white background, ~2.2 MB. Roman/Mughal
  base colours are swapped vs the band tokens. NOT wired in — waiting on the
  user (see manifest).
- Retired gangster files still on disk: `tiles/{first-city-bank,golden-chip,
  onyx-room,shipping-port}.svg`, empty `bosses/`. Delete only with the OK.
- `/design-review` on v3. First playtest must re-test teachability.

---

# Session State — 2026-08-04

## Current task

**ADR-0005 game-module interface refactor — done and verified.** The platform
is now game-agnostic; Empire Wars (game #2) is unblocked and next.

## Progress checklist

- [x] `lib/game/module.ts` — `GameModule<TState,TView,TMove>` + `Standing` +
      `eraseModule` (type erasure, casts contained and documented)
- [x] `lib/game/registry.ts` — server dealer registry, keyed by `gameType`
- [x] `lib/game/nuno/module.ts` — Nuno adapter; **`nuno/rules.ts` untouched**
- [x] `lib/game/session.ts` — `persistGameState` takes the module;
      `buildLineup` split into module `standings()` + `decorateStandings()`
- [x] `lib/actions/games.ts` — generic `runTransition`; `submitMove` replaces
      `playCard`/`drawCard`/`passTurn`/`callNuno`; dealer via registry
- [x] `components/game/boards.tsx` + `types.ts` — client board registry
- [x] Nuno board moved: `app/(platform)/lobby/[id]/nuno-board.tsx` →
      `components/game/nuno/board.tsx`
- [x] `game-screen.tsx` / `lobby-room.tsx` / both pages — game-agnostic props
- [x] `tests/game-module.test.ts` — 14 contract tests, `describe.each` over
      every registered module
- [x] lint / typecheck clean; **76 tests pass** (62 existing + 14 new)
- [x] Browser-verified live: deal → CPU turn → play → wild+color → quit →
      standings ribbon → lobby reopen + ready reset

## Key decisions this session

- **ADR-0005 written** (`docs/architecture/adr-0005-game-module-interface.md`).
  ADR-0004's "catalog entry + rules module + dealer branch" was optimistic —
  five platform surfaces were hard-typed to Nuno. All five are now generic.
- **`Standing[]` is the platform's only view of outcome**: `{ memberId,
  detail?, eliminated? }`, `detail` free text owned by the game. This is what
  lets `GameOverModal`, the lobby last-hand ribbon and `GameWin` all work for
  any game. Standings logic previously existed twice (session.ts +
  game-screen.tsx); now once.
- **`module` is a forbidden variable name** — `@next/next/no-assign-module-variable`.
  Use `gameModule`.
- **Interface assumes turn-based, one seat on the clock.** Empire Wars'
  auctions and pay-or-fight windows must model themselves as a pending-decision
  phase with a deadline *inside* game state — serverless gives no server clock.

## Complete — Empire Wars GDD

`design/gdd/empire-wars-core.md` — **all 11 sections written and approved**
(1,085 lines): Overview, Player Fantasy, Detailed Rules, Formulas, Edge Cases,
Dependencies, Tuning Knobs, Acceptance Criteria, Visual/Audio, UI, Open
Questions. Authored via `/design-system`, section-by-section with approval.

Locked design: 32 tiles (8 districts x 3 + 8 specials), 20 rounds, 3
currencies + Heat meter, action die *offers* rather than compels, refusing
protection costs fee+Respect only (turf war takes land), crew pool-vs-garrison
split, dice-pool combat (best 2 of N d6, ties to defender), Heat +1 above
1.25x table average ("Most Wanted"), raid confiscates Dirty Money only, jail
resets Heat to 2, broken-not-bankrupt, Kingpin OR round-20 Empire Value, one
pending decision at a time (20s window, lazy client-driven resolution),
Respect+Heat public / Cash+Dirty+missions private.

Full board economy is specified with concrete numbers: $10,010 of land, income
generated from `price x TIER_RATE x LEVEL_MULT`, all 24 territories priced.

### DONE — ADR-0005 Amendment 1 (2026-08-04)

`MoveContext = { random, now }` now threads through every **transition**:
`deal`, `apply`, `quit`, `botMove`. `viewFor` deliberately stays pure — a
countdown is derived client-side from a deadline carried in the view.
`moveContext()` in `lib/actions/games.ts` is the ONLY place `Math.random` and
`Date.now()` enter the game layer, stamped once per transition.

Closed the latent Nuno defect: `lib/game/nuno/module.ts` called
`applyDraw(state, memberId)` with no `random`, so the discard reshuffle ran on
`Math.random()` inside a pure transition. Now passes `ctx.random`.

Test coverage added (81 tests total, was 76):
- Generic per-module: same seed deals identically; a whole bot-played game
  replayed from one seed produces an identical final state.
- Nuno regression: drained-deck draw twice under one seed must match.
  **Verified this test FAILS against the pre-amendment adapter** and passes
  after — it is a real regression test, not a tautology.

Verified live: dealt a hand, drew (the changed line), CPU turn, played a card,
quit. All correct. ADR-0005 Amendment 1 marked implemented.

### DONE — seat-colour bug + asset path (2026-08-04)

Found by the external design review, verified independently, fixed. THREE
defects, not one:

1. **Collision** — 10 avatars mapped onto 5 hues, two per hue, so two players
   picking "High roller" and "Table bear" both rendered gold at one table.
2. **Wrong source** — `AvatarChip` derived colour from the chosen avatar, never
   from `LobbyMember.seat`. Both art-direction guarantees ("in seat order",
   "player 1 is always gold") were false.
3. **Missing sixth colour** (review did NOT catch this) — seats run 1..6
   (`LOBBY_LIMITS.maxPlayers`) but only `--chart-1..5` existed, so seat 6
   rendered `var(--chart-6)` = undefined at a full Nuno table.

Fix: added `--chart-6` (rose, `oklch(0.75 0.14 345)`); renamed
`AvatarDef.seat` -> `AvatarDef.hue` (the misnomer was the root cause);
`AvatarChip` gained an optional `seat` prop that wins when present.
`BoardMember`, `WinLineupEntry` and `GameStanding` now carry `seat`.

**Deliberate call**: chat avatars keep the avatar hue on ALL surfaces — a
message stream is not a seat display, and the realtime `chat-message` payload
carries no seat (changing that needs an ADR-0001 schema change). Recorded in
`design/art-direction.md`.

Also: `public/games/empirewars/` -> `public/games/empire-wars/` to match the
catalog's `public/games/<id>/` convention and the `empire-wars` game id. GDD
lines describing the old path updated.

Verified: lint/typecheck clean, 81 tests pass, and confirmed live — the same
player renders sky in the top bar (off-table hue) and gold in the lobby seat
grid (seat 1) in one screenshot.

NOTE: the logo is still 6.3 MB and wants re-encoding to 512x512.

### IN PROGRESS — Empire Wars implementation (autonomous /loop)

Phases 1 and 2 complete. 143 tests pass, lint + typecheck clean.

Built:
- `lib/game/data/empire-wars.ts` — full tuning surface, verified against the
  GDD tables (board total $10,010, all 120 income combos regenerate).
- `lib/game/empire-wars/rules.ts` — zod state, board lookups, dealGame, all
  pure formulas.
- `lib/game/empire-wars/economy.ts` — movement, income, laundering, buy/
  decline, build/upgrade/recruit, protection, upkeep+raids+seizure, Most
  Wanted sweep, broken-not-bankrupt.
- `lib/game/empire-wars/simulate.ts` — economic match simulation.
- `tests/empire-wars-rules.test.ts` (32) + `tests/empire-wars-economy.test.ts` (30).

TWO DESIGN DEFECTS FOUND AND FIXED (both measured, not guessed):

1. **Heat brake never engaged.** Most Wanted (+1/round) and the clean-upkeep
   shed (-1/round) cancelled exactly. Fixed: shed is suppressed while Most
   Wanted (`HEAT.cleanShedAppliesToMostWanted = false`). GDD updated.

2. **Every upgrade was economically irrational.** Payback 5.4-27.8 laps in a
   ~4.3-lap match; every upgrade EV-negative on purchase. The whole 5-level
   property system was dominated by never upgrading. Fixed: territory income
   collects EVERY ROUND (`EMPIRE_WARS_CONFIG.incomeEveryRound = true`); the
   Docks stipend stays lap-based. GDD updated.

   Measured after: leader EV $10,714 (GDD target $10-14k), leader/mean 1.35x
   (threshold 1.25x), reaches Raids band 68%, seizure fires. BOTH fixes were
   required — either alone leaves the leader untouched.

Also found earlier: mission deck is 10 cards but 6 seats x 3 = 18, so decks
are per-player (each seat shuffles its own copy). Needs user confirmation.

### NEXT — Phase 3

Movement/tile resolution wiring, combat (standoff/strike/turf war) + the
Monte-Carlo win-rate table that fills the GDD's `?` cells and then decides
LEVEL_DEFENCE, pending decisions with server-side expiry, jail/bribery/
districts/boss passives/missions/events/truce, victory + standings.
Then Phase 4: module.ts + registry + catalog, bot, board UI.

### Phase 3 + module wiring DONE (autonomous /loop)

193 tests pass, lint + typecheck clean. **Empire Wars is registered and passes
the full platform contract** — bots play a complete match to a winner with no
rejected moves, and seeded replay is deterministic.

New files:
- `lib/game/data/empire-wars-content.ts` — 10 missions, 8 personal events
- `lib/game/empire-wars/combat.ts` — standoff / strike / turf war / truce
- `lib/game/empire-wars/turn.ts` — turn engine, 20 moves, pending decisions
  with SERVER-side expiry checking
- `lib/game/empire-wars/view.ts` — per-viewer projection
- `lib/game/empire-wars/bot.ts` — utility heuristics, answers every decision
- `lib/game/empire-wars/module.ts` — GameModule adapter
- registered in `lib/game/registry.ts` + `lib/game/catalog.ts`
- `tests/empire-wars-combat.test.ts` (32)

COMBAT TABLE COMPUTED EXACTLY (full enumeration, not sampling). **12 of the
GDD's 16 published cells failed its own +/-2pp criterion** — estimates were too
generous to the attacker whenever the defender had 3+ dice, by up to 6pp
(5v5 published 47%, actual 41.0%). GDD table replaced with exact values.
DECISION: `LEVEL_DEFENCE` stays — a Fortress (6 dice) needs ~8 attacking crew
for a coin flip (48.6%), comparable investment on both sides, and the working
Heat brake stops a Fortress-heavy leader sitting unbothered.

TWO MORE DEFECTS FOUND BY THE CONTRACT SUITE:
1. **Declining a tile was repeatable** -> a bot re-evaluating the same unowned
   tile declined forever and no match could finish. Added `state.tileDone`.
2. **The contract's "view smaller than state" leak heuristic was wrong** for a
   game whose state is mostly public — Empire Wars' view denormalises board
   data and is legitimately larger. Replaced with per-viewer-difference, and
   added precise leak tests (no other seat's cash/dirty/in-hand missions).

### Missions + events DONE — 219 tests pass

`lib/game/empire-wars/progress.ts` evaluates mission completion and applies
event effects; both wired into `endTurn` / the Question die / The Speakeasy.
`tests/empire-wars-turn.test.ts` (26) covers move validation, tile settling,
pending-decision expiry (incl. the server-clock check), missions, events,
quitting, and a full bot-driven match to a winner.

Added 3 player counters missions need that CANNOT be derived from the board:
`fightsWon`, `turfWarsWon`, `launderedTotal`.

### BOARD UI DONE — Empire Wars is PLAYABLE END TO END

`components/game/empire-wars/{board,tile,heat-track,decision-overlay}.tsx`,
registered in `components/game/boards.tsx`. `--ew-*` tokens added to
globals.css (non-theme-scoped, like Nuno's deck).

Verified live in a real lobby: dealt from the shelf, 32-tile ring rendered
with district bands + special tiles, bot bought Gin Joint autonomously, I
rolled (moved 8 to County Jail), the action die came up Handshake and OFFERED
a truce, the bot ANSWERED the pending decision ("Truce declined" — correct,
it only accepts from stronger players), build phase, end turn, bot declined
Pawn Shop and the price marked down 120 -> 108 exactly as specced. Quit ->
"Croupier won the hand · Touhidul Islam Chayan (walked away)", lobby reopened.
No console errors, no server errors during the playthrough.

Board sizing: the ring is capped at `max-w-[min(100%,38rem)]` — an uncapped
aspect-square grid in the wide column is taller than the viewport.

### EMPIRE WARS v1 COMPLETE (2026-08-04)

Rules, bot, UI, content, docs — all shipped and verified live. 219 tests,
lint + typecheck clean.

Final pass added:
- City Hall (3 bribe tiers + buy Respect) and jail bail actions in the rail.
  The rules supported them all along; the UI never surfaced them.
- `design/gdd/empire-wars-bot.md` — the companion spec the GDD asked for.
- Assets compressed to catalog spec: logo 2000x2000/6.0MB -> 512x512/110KB,
  cover 1672x941/2.2MB -> 1280x800/596KB. **7.6MB saved.**

### WHAT IS DONE

Platform: ADR-0005 game-module interface + Amendment 1 (ctx), board registry,
seat-colour fix (+ --chart-6), Nuno reshuffle defect closed.

Empire Wars: data, rules, economy, combat, turn engine, progress
(missions/events), view, bot, module, board UI (4 components), content.
Registered in both registries + catalog. Playable end to end in a real lobby.

### NOT DONE — deliberate, needs a human

- **Nothing is committed.** ~45 files. This is the biggest outstanding item.
- **Playtesting.** Every balance number is simulation-verified, not
  human-verified. The GDD's open questions (real wall-clock match length, is
  Kingpin reachable, does the auction-less mark-down keep the board moving)
  can only be answered by playing it.
- **Re-review.** The GDD changed materially since the last review (income
  cadence, Heat shed, exact combat table). Worth `/design-review` in a fresh
  session.
- Richer motion (the GDD specifies dice tumble, token stepping, raid wash,
  district-completion flourish) — the board is functional but static.
- Audio: specified, not implemented; the platform has no audio layer.
- Mission decks are PER-PLAYER (10 cards can't deal 3x6 from a shared deck).
  Still wants user confirmation.

### UI PASS + THREE REAL BUGS FIXED (2026-08-10)

232 tests, lint + typecheck clean. Verified live.

UI work (all five requests):
1. Everything now lives INSIDE the felt — rivals strip along the top, board in
   the middle, your rail along the bottom. Nothing outside the table.
2. `design/empire-wars-art-manifest.md` — 37 files with paths/sizes/subjects.
   Tile art is picked up via `TILE_ART` in `components/game/empire-wars/tile.tsx`
   (a manifest, not a filesystem check — `<Image>` on a missing src 404s).
3. Remix icons for Cash/Dirty/Respect/Crew/Empire/Heat and the 6 action-die
   faces. NOT emoji — those render differently per platform.
4. Player dots -> real pieces: fedora, roadster, tommy gun, tumbler, cigar,
   pocket watch (inline SVG, seat-coloured, `icons.tsx`).
5. Dice tumble (~620ms) + pieces walk tile-by-tile with a hop (120ms/step).
   Both honour prefers-reduced-motion. `dice.tsx`, `pieces.tsx`, keyframes in
   globals.css.

Also added `lastRoll` to state — the dice values were never stored, so a
player could not see what they rolled.

THREE BUGS FOUND WHILE TESTING:

1. **Lobby permanently wedged (production hazard).** A live GameSession whose
   state predates a schema change fails `parseState` -> game route bounces ->
   `startGame` refuses (status != OPEN) -> the lobby can never be played or
   reset. This WILL happen on any deploy that grows game state. Fixed both
   layers: every later-added field now carries `.default()`, and `startGame`
   retires an unreadable session and reopens the lobby.
2. **Turn stranded with no legal move.** An automatic die face (Money Bag /
   Police / Question) cleared `dieFace` without advancing the phase, leaving a
   human in `act` with zero buttons. The BOT hid this for weeks because
   `chooseBotMove` falls through to `endTurn` from any phase.
3. **"Nuno in progress"** shown for an Empire Wars game — the lobby banner used
   `availableGames()[0]`, correct only while Nuno shipped alone. Now reads
   `lobby.gameType`.

All three have regression tests; #2's was verified to fail without the fix.

### GUIDED UX PASS (2026-08-10) — "too complex, not fun"

240 tests, lint + typecheck clean, verified live.

DIAGNOSIS: the rules complexity was approved design, but the UI gave ZERO
guidance — 39 Button references, 16 concepts, every action always visible at
equal weight, nothing explaining what anything did. The GDD's own open
question "can a first-timer be taught this in 3 minutes?" was answered: no.

FIX — a teaching layer, not a rules change:
- `lib/game/empire-wars/coach.ts` (pure, view-driven) picks ONE obvious next
  action per moment, states the consequence in plain words, and demotes the
  rest. e.g. "Chop Shop is for sale / Pays you $63 every round" + one Buy
  button, with Pass small underneath.
- `components/game/empire-wars/rail.tsx` enforces one-primary-action-ever and
  renders nothing that doesn't apply right now. Build phase went from 8
  always-on buttons to 2 relevant ones + End turn.
- Every stat carries what it is FOR: "buys turf", "earns more draws police",
  "counts at the end", "wins fights", "what wins the game".
- Heat band explained in words: "Clean — nobody's looking at you" /
  "Raids — the police may take your dirty money, or a tile".
- First-run "Running the city" card: the whole game in 4 lines, auto-opens
  once, then a "How to play" link.
- `TerritoryView` gained `income` + `districtName` so choices can show
  consequences.
- board.tsx shrank 620 -> 449 lines.

`tests/empire-wars-coach.test.ts` (8) proves the coach NEVER suggests a move
the rules reject (walks whole turns following only its advice) and always
offers a way forward when it is your turn.

BUG I INTRODUCED AND FIXED: reading localStorage in a `useState` initialiser
caused a real hydration mismatch (server said "seen", client said "not seen").
Effect + setState trips the lint rule; the correct tool is
`useSyncExternalStore` with a server snapshot. Verified gone from the console.

STILL OPEN: this is a UX fix, not a rules cut. If it still feels heavy after a
playtest, the honest next lever is cutting systems (strikes, truces, enforcers
and bribery are all candidates) rather than more explaining.

### CPU STALL BUG FIXED + TURN CLOCK (2026-08-10)

247 tests, lint + typecheck clean, both verified live.

THE BUG — "Croupier is thinking…" forever.
`advanceBot` plays ONE move, but a CPU turn is several (roll, buy, upgrade,
end). The client driver's effect was keyed on
`[onClockIsBot, onClock, view.round, view.phase, view.winnerId, ...]` — and
after the bot's first move NONE of those change (buy/recruit/upgrade all keep
the same phase and round). The effect never re-fired, so the driver stopped
calling and the table hung with a CPU on the clock. Nuno never hit this only
because its deps included `turnCount` and `deckCount`, which move every play.

FIX: `state.version`, bumped on EVERY applied move in `applyMove`, exposed on
the view, and used as the effect dep. Three regression tests, all verified to
fail without the bump — including one that does two moves in the same
phase+round and asserts the version still differs.

TURN CLOCK (requested): `EMPIRE_WARS_CONFIG.turnWindowMs = 30_000`.
- `state.turnExpiresAt` refreshed on every move by the seat on the clock.
- New OPTIONAL `turnExpired(state, now)` on the GameModule interface — a
  genuine platform concern ("a seat that isn't answering must not freeze the
  table"). Nuno omits it and is unaffected.
- `advanceBot` now plays for a HUMAN seat too, but only once
  `turnExpired` says so, checked against the SERVER clock (a fast client
  cannot jump someone's turn).
- Client schedules the call with the same jitter as the decision resolver.
- CPUs never touch this path — they run on botDelayMs as before.
- Countdown shown only in the last 10s (a clock ticking all turn makes a
  casual game feel like an exam).

VERIFIED LIVE: CPU completed a full multi-move turn ("Passed on Car Wash —
marked down", tile marked $180 -> $162, piece moved) and handed back. Then I
sat idle and the auto-roll fired on its own — dice 3+2 + Money Bag, piece
moved to Garage, rail offered "Garage is for sale".

### "Hire crew" crash — ROOT CAUSE WAS RENDER LOAD (2026-08-10)

249 tests, lint + typecheck clean. Verified live: Hire crew works
($1,500 -> $1,250, crew 2 -> 3), zero P1017 since.

The action itself was FINE — the log showed `submitMove(recruit)` returning
200. What crashed was a *subsequent page render* (P1017 "Server has closed the
connection"), which the error boundary showed as "The table wobbled".

ROOT CAUSE: `refreshGame()` revalidated `/lobby/[id]` and `/dashboard` but
NEVER `/lobby/[id]/game` — the page you actually look at while playing. So the
server action's own response carried no fresh data for that route, and every
call site compensated with `router.refresh()`: a SECOND full render (4+
sequential queries) for every single move. Harmless while the bot driver was
broken and fired once; the moment the version fix made it fire every 1-3s it
became several redundant renders per second against a 5-connection local pool.

FIXES:
- `refreshGame` now revalidates the `/game` route (this was also a latent
  correctness bug — that route was never revalidated server-side).
- Dropped all four `router.refresh()` calls from the Empire Wars board. Next
  docs: revalidatePath in a Server Function "Updates the UI immediately".
- Game page: `getOnboardedUser` + `getLobbyWithMembers` now run in parallel.

NOTE: `components/game/nuno/board.tsx` still has the same redundant
`router.refresh()` pattern. Harmless (Nuno is one move per turn) but worth
cleaning up for consistency.

TWO MORE BUGS FOUND WHILE VERIFYING:
1. **Hydration mismatch, mine.** `TurnClock` and the decision overlay's
   countdown both seeded from `Date.now()` during render, so server and client
   wrote different numbers. Both now start null and fill in on the first tick.
2. **Stale buy offer.** `buyableAt` ignored `state.tileDone`, so after a pass
   the coach kept offering "…is for sale" and the server rejected the click
   with "You've already settled this tile" — reads as a broken button. Now
   respects tileDone and only offers to the seat on the clock. 2 tests.

### Next steps

1. ~~Design review~~ — run by the user in a separate session (findings not
   yet folded back into the GDD; ask before building on it).
2. ~~ADR-0005 Amendment 1~~ — DONE, see above.
3. **NEXT**: `/ux-design` for the game surface and decision overlay before
   writing stories (flagged in the GDD's UI section).
4. Then build: `lib/game/empire-wars/{rules,module}.ts`,
   `lib/game/data/empire-wars.ts`, `components/game/empire-wars/board.tsx`,
   registry + catalog entries.

Open risks recorded in the GDD: combat win-rate table is an ESTIMATE pending a
Monte-Carlo test; wall-clock match length at 6 seats unmeasured; the bot is the
largest schedule risk and is v1 scope.

NOTE: `design/gdd/game-concept.md` and `design/gdd/systems-index.md` do not
exist (never did — Nuno shipped without them), so there is no systems index to
update. `design/registry/entities.yaml` is still all commented-out examples.

## Original plan — Empire Wars (game #2)

Approved scope: **staged v1 core**, then v2. All nine of the proposed rule
changes were approved, notably: Heat rises with wealth (anti-runaway-leader),
"broken not bankrupt" (no dead players), 3 currencies not 4 (Influence folded
into Respect), dirty money as a hot potato with a Heat clock, combat as a
dice pool (Nd6 keep best 2), every blocking decision has a timer + default,
32-tile board (8 districts x 3), truce/betrayal via the Handshake die face.
v1 defers: Black Market, vehicles/weapons economy, full alliances, turf wars,
informants. **The bot is v1 scope, not optional.**

Start with `design/gdd/empire-wars-core.md` (8 required sections, authored
section-by-section with user approval) — not code.

Assets already in place: `public/games/empirewars/{empirewars_logo.png,
cover.png}`. Both are oversized (6.3 MB / 2.3 MB) and want compressing; the
cover is 1712x941 vs the 1280x800 spec, so it crops slightly in
`aspect-[16/10]` cards.

---

# Session State — 2026-07-27

## Current task

Dual-sidebar (left nav + right crew rail) shell across `/`, `/dashboard`,
`/lobbies`, `/friends`, `/player/[username]` — **done and verified**.

## Progress checklist

- [x] `components/shell/` — `app-sidebar` (left nav, signed-in + signed-out
      variants), `right-rail` (crew/notifications, hidden on `/lobby/*`),
      `online-crew`, `search-box`
- [x] `app/(platform)/layout.tsx` — shell wraps all platform routes
- [x] `app/page.tsx` — landing rebuilt as public shell variant (auth CTAs,
      live stats + seat-color legend in right rail)
- [x] Top-bar search → `/friends?q=…`; `FriendSearch` auto-runs initial query
- [x] typecheck / lint / build pass
- [x] Browser-verified: landing (signed out) at 1440 + 1280; dashboard,
      lobbies, friends, profile (signed in)

## Key decisions / fixes this session

- **`lib/db.ts` pool fix**: local `prisma dev` server resets connections
  beyond ~7 concurrent (reproduced with a standalone pg script). Dashboard +
  Friends burst past that (layout 3 + page 3+ parallel queries) → P1017
  "Server has closed the connection". Pool now capped at
  `DATABASE_POOL_MAX` (default 5) with `keepAlive: true`. Requires dev-server
  restart to take effect (Prisma client is cached on `globalThis`).
- **`lib/locks.ts` advisory-lock fix**: `pg_advisory_xact_lock()` returns
  Postgres `void`, which the Prisma 7 pg driver adapter cannot deserialize
  from a `$queryRaw` result (P2010 UnsupportedNativeDataType — broke
  sendFriendRequest). Switched to `$executeRaw` (row count only, no column
  deserialization). Verified live: chicken → jenga request now succeeds.
- **Mutation → UI revalidation sweep** (UX): `updateProfile` now does
  `revalidatePath("/", "layout")` (chip/name render in the shell everywhere);
  friends `refresh()` also revalidates `/player/[username]` pages;
  `refreshLobby()` also revalidates `/dashboard` (active-table banner);
  `createLobby` uses `refreshLobby`. `FriendActionButton` keeps an
  optimistic override keyed to its incoming props so client-held search
  rows flip instantly (send/cancel/accept/remove); `sendFriendRequest`
  returns the new `friendshipId` to enable the none→outgoing flip.
  Browser-verified both directions + chip change reflecting in the top bar.
  Incoming requests now render an Accept + Decline pair (decline was a
  UI-less server action before); verified live with a seeded request.
- **Shell/UX round 2**: "Table" renamed to "Lobby" across nav, pages, room,
  and action errors (flavor idioms like "Take a seat"/"The table wobbled"
  kept). New `components/shell/lobby-quick-panel.tsx` at the top of the
  crew rail — active lobby: name/code + copy, seats, Enter lobby, locked
  Start game, quick-invite of unseated friends; no lobby: Quick setup
  (one-tap create + join by code). `getActiveMembership` now includes
  member count + ids. New `components/notification-row.tsx` (per-type icon
  chip, relative time, unread dot) used by the bell popover and the rail's
  Latest list. RailChat added to the quick panel: same ephemeral Pusher
  channel as the room's Table talk; history persists across soft navigation
  (the shell layout stays mounted) and resets inside /lobby/* where the
  room chat takes over. Non-chat lobby events refresh the rail, keeping
  seat counts live. Verified both directions (rail ↔ room).
- **Lobby card redesign (rail)**: seat grid shows who's at the table
  (avatar chips + dashed Open seats, tiny names — chip motif per art
  direction); code pill copies itself; invite list moved into a popover;
  clear sections (header / seats / actions / chat). `getActiveMembership`
  members now include user + seat details; `ActiveLobbySummary` carries
  `members` instead of counts/ids.
- **Game-over modal + lobby history**: new `components/ui/dialog.tsx`
  (Base UI Dialog wrapper, matches popover.tsx conventions) and REUSABLE
  `components/game/game-over-modal.tsx` — takes `{gameName, gameLogo,
  standings[], youId, onPlayAgain?, onClose}`, i.e. game-agnostic (a title,
  a logo, a ranked list) so game #2 just passes its own standings.
  game-screen no longer auto-redirects after 3s; the finished hand stays
  behind the modal. CRITICAL companion fix: the game PAGE used to redirect
  as soon as the session ended — added `getRenderableGameSession()` with a
  5-min grace window (`GAME_RESULT_GRACE_MS`) so a just-finished session
  still renders. GameWin gained `lobbyId` + `players` (frozen lineup JSON,
  migration `20260729060000_game_win_lineup`), written by
  `buildLineup()` in lib/game/session.ts; member queries in games.ts +
  lobbies.ts now include user details for it. Lobby room shows "Table
  history" (winner, game, who they played with) when no game is running;
  profile trophy shelf unchanged.
  VERIFIED: profile shelf + history data path (via a temporary seeded
  GameWin row, since only bots had won — row deleted afterwards, GameWin
  table back to 0). NOT yet seen live: the modal itself and the lobby
  history section — the test hand was still running when I stopped.
- **BOT STALL BUG FIXED**: the bot driver effect keyed only on
  `view.turnCount`, but a bot that draws a PLAYABLE card keeps its turn
  (turnCount unchanged) → effect never re-fired → table frozen mid-turn
  forever. Added `view.deckCount` to the deps. This was a genuine
  game-freezing bug, reproduced live twice.
- **Animation glitch fixes** (play path): the played card used to reappear
  in hand before the refresh removed it. Causes + fixes: (1) a 1200ms
  "safety" timer un-hid the card before `router.refresh()` landed — removed;
  the hidden state is now DERIVED from `pending && card still in hand`, and
  `pending` spans the action *and* its refresh, so it self-corrects (a
  rejected play restores the card; a reshuffled duplicate id can't stick
  invisible). (2) The landed card unmounted while the pile still showed the
  old top → blink. Flights now take `holdForTopId` and park on the pile
  until the server confirms that id as top (derived `visibleFlights`;
  2s backstop sweep for rejected plays). (3) The slot now collapses
  (width/margin/opacity, 200ms) so neighbours slide together during the
  flight — nothing shifts when the refresh lands. (4) Drawing shows a
  NUNO card BACK holding the slot until the real card arrives (derived
  from `deckCount` being unchanged — no timers). Bot advance now retries
  once on failure (a failed advance otherwise froze the table, since the
  effect only re-runs on turn change).
  NOTE: editing hooks in this file while a game page is open corrupts it
  via Fast Refresh ("useEffect dep array changed size") and stalls the bot
  — reload the page after such edits; not a shipped bug.
- **Card motion** (`components/game/card-flight.tsx`): cards fly hand→pile
  and deck→hand. Motion spec from the motion-design skill — spatial morph,
  `ease-in-out-cubic` @ 240ms, transform/opacity only, `prefers-reduced-
  motion` skips flights entirely. Own plays animate OPTIMISTICALLY on click
  (before the server round trip) and register in `animatedPlays` so the
  confirmed state doesn't replay them; opponents' plays/draws are detected
  by diffing topCard id + per-seat card counts against `prevSnapshot`.
  Draw fan-outs stagger 70ms. Action cards also raise a 2.6s announcement
  ("X played +4 — Y draws 4") so penalties are visible.
- **RULES BUG FIXED**: a +2/+4/reverse played as the winning card returned
  before its effect ran, so the next player never drew. Now the effect
  resolves first, then the win — matches the rulebook and keeps placement
  counts honest. Regression tests + new `tests/nuno-simulation.test.ts`
  (100 full games over 2/3/4/6 seats asserting card conservation, exact
  +2/+4 amounts on the right seat, skip/reverse/wild behaviour,
  termination). 62 tests total.
- **Nuno gameplay UI**: reusable `components/game/nuno-card.tsx` —
  `NunoCardFace` (white frame, tilted oval + dashed chip rim, outlined
  glyph, corner glyphs, four-colour chip for wilds), `NunoCardBack` (indigo
  + gold oval + NUNO wordmark), `NunoCardFan` (overlapping backs; uses
  FIXED px overlaps — percentage margins don't resolve in flex). Sizes
  xs/sm/md/lg. Board rebuilt as a table: opponents ringed by slot map
  (south seat is always the viewer; 2 opponents → top-left/top-right),
  piles centre, POV hand on the felt's south edge with nameplate. Playable
  cards ring gold + rest raised; unplayable are opacity-only (filters read
  as "broken"). "No plays — draw a card" state pulses the draw pile.
  NOTE: card oval geometry is being tuned by the user directly — do not
  overwrite `nuno-card.tsx` oval values without checking with them.
- **Bug fixed — hidden seats**: `updateLobbySettings` guarded seat count
  but not seat NUMBER, so shrinking max below an occupied sparse seat
  (member at seat 4, max→2) hid that player from the grid entirely
  ("3/2 seated"). Now floors on the highest occupied seat; the room grid
  also renders `max(maxPlayers, highest seat)` slots. chicken's table data
  repaired (max 2 → 4).
- **DB pool hardening** (`lib/db.ts`): added `maxUses: 50`,
  `idleTimeoutMillis: 10s`, `maxLifetimeSeconds: 120`. Aborted RSC renders
  (router.refresh / fast navigation) can leave a socket protocol-desynced
  → 08P01 "bind message supplies N parameters, but prepared statement
  requires 0" on every reuse. Retiring sockets fast limits the blast
  radius. The local `prisma dev` server itself also wedges under load
  (all connections ECONNRESET) — restart `npm run db:dev` then the app;
  data survives in %LOCALAPPDATA%\prisma-dev-nodejs.
- **Game content system**: catalog entries carry `logo` (square, sidebar +
  chrome) and `cover` (card backgrounds). Shared `components/game-cover.tsx`
  renders cover-art cards (bg image + legibility gradient + logo/name/
  caption) on the home shelf and lobby "Pick a game" grid; missing cover
  degrades to plain card color. ASSET SPECS for user: logo 512×512 PNG
  transparent at `/games/<id>/logo.*`; cover 1280×800 (16:10) at
  `/games/<id>/cover.png` — Nuno's cover path is pre-wired, drop the file
  and it appears. Catalog now contains ONLY Nuno (placeholders removed per
  user). Host "Table" settings popover (name/visibility/seats 2–6) +
  per-game seat-fit checks on cards.
- **Lobby game shelf**: the room's "Start Nuno" button replaced with the
  same catalog card grid as the dashboard ("Pick a game" — host clicks a
  card once everyone's ready; non-hosts see "The host starts the game").
  Placement banner shows "(quit)" for eliminated players. Also fixed:
  post-`db push` stale pooled prepared statements (08P01) → dev-server
  restart required after schema pushes; eliminated players show "out" in
  the board strip.
- **Platform-of-games architecture (ADR-0004, Plato-style)**: lobby is the
  persistent group; `lib/game/catalog.ts` is the single game registry
  feeding sidebar GAMES, home-page shelf, and `startGame(lobbyId, gameId)`.
  Full-screen game surface at `/lobby/[id]/game` (sidebar hides there;
  board + game chat side by side; seated players auto-pulled in). Game
  chat = `LobbyMessage.sessionId` + event `scope:"game"`, wiped at game
  end. Quit = elimination (`applyQuit`/`quitGame`; leaving the lobby quits
  first; last human standing → abandoned session, no record). `GameWin`
  model = only persistent record; written for human winners in
  `lib/game/session.ts` `persistGameState` (centralized end-of-game
  contract). Profile: Wins stat + Trophy shelf. Migration
  `20260727070000_game_wins_and_game_chat`. 56 tests pass. NOTE: quit/
  game-chat/win-record need a live 2-human hand for e2e — unit-covered.
- **Nuno — first official game (ADR-0003, design/gdd/nuno-core.md)**:
  UNO-clone per user spec, 2–6 seats (LOBBY_LIMITS.max now 6). Pure rules
  `lib/game/nuno/rules.ts` + knobs `lib/game/data/nuno.ts`; 52 tests incl.
  full deck/actions/Nuno-call/win coverage (`tests/nuno-rules.test.ts`).
  Server actions `lib/actions/games.ts`: startGame/playCard/drawCard/
  passTurn/callNuno/advanceBot/fillSeatsWithBots — all under lobby lock;
  hidden info via `viewFor` per-viewer projection (clients never see other
  hands/deck). Bots: client-scheduled `advanceBot` after 1–3 s (config),
  server-validated; departed seats play as bots. Board UI
  `app/(platform)/lobby/[id]/nuno-board.tsx` (logo at
  public/games/nuno/nuno_logo.png, CSS cards, wild color picker, NUNO!
  window button, placements banner). High Roll fully removed (ADR-0002
  Superseded). LIVE: hand dealt at chicken's table, chicken played
  yellow 6, waiting on jenga.
- **High Roll test game (ADR-0002, superseded)**: first game through the pipeline —
  pure rules `lib/game/high-roll/rules.ts` + knobs `lib/game/data/high-roll.ts`
  (unit tests `tests/high-roll-rules.test.ts`, 35 total passing). Server
  actions `lib/actions/games.ts` (startGame host-gated on all-ready ≥2;
  submitRoll one per seat; both under the lobby advisory lock; bots roll at
  deal). State in `GameSession.state` (zod-validated on read); `game-changed`
  hint event added to the lobby schema; closing lobby force-ends open
  sessions. Room UI: roll values replace ready pills while IN_GAME, Roll
  button, waiting line, winner banner from last ended session, Start button
  wired (host-only). LIVE STATE: round in progress at "chicken's table" —
  chicken rolled 3, Croupier (CPU) 2, waiting on jenga's roll to complete.
- **Chat history now synced/persisted**: new `LobbyMessage` model (schema +
  hand-authored migration `20260727050000_add_lobby_message` for the cloud
  DB; local used `db push`). `sendLobbyChat` writes the row, then publishes
  the event with the DB id; both chat surfaces seed from
  `listLobbyMessages()` and dedupe live events by id. Messages are deleted
  when the lobby closes ("messages vanish" promise kept). ADR-0001 addendum
  records this. NOTE: local `prisma dev` DB was restarted this session (it
  wedged — connections reset under any concurrency); data persists in
  %LOCALAPPDATA%\prisma-dev-nodejs.
- Hydration-mismatch warning on `<body>` (`cz-shortcut-listen`) is a browser
  extension artifact, not an app bug.
- `.next/dev/types/validator.ts` can be corrupted by a mid-write race —
  touch any app file to regenerate before trusting typecheck failures there.

## Open questions

- User mentioned inspiration images that never arrived in chat — shell built
  from existing art direction instead. Revisit if they share them.
